# SafeShip 기획서 (v0.1)

- **작성일:** 2026-07-08
- **작성자:** coolk (cool0720zzz@gmail.com)
- **버전:** v0.9 (내 은하 대시보드 + 브리핑 게이트 추가; v0.8 = 포지셔닝 확정)
- **문서 성격:** 이 문서는 이후 모든 작업 에이전트가 준수해야 하는 **구속(binding harness) 명세서**다. 벗어나야 하면 게이트로 복귀해 이 문서를 갱신하고 버전을 +0.1 한다.
- **코드네임:** `SafeShip` (한글 임시명 "안전배포 관제탑") — 확정 아님, 변경 가능.
- **지식 베이스:** `research/research-git-safety.md`, `research/research-cost-guard.md`, `research/research-deploy-platforms.md` (전 플랫폼·전 프로바이더 규칙 데이터. 룰 레지스트리의 원본).

---

## 1. 왜 (목적·성공 기준)

### 문제
바이브코딩(AI로 코드 생성)하는 **비개발자**가:
- Git 브랜치/커밋/푸시 개념 없이 `main`에 직접 커밋하고, 되돌릴 수 없는 사고를 낸다.
- `.env`·API 키를 커밋하거나 클라이언트 번들에 노출한다 (실제 $3천~$73천 크립토마이닝 청구 사례).
- 배포 전에 뭘 확인해야 하는지 몰라 깨진 배포를 낸다 (출력 디렉터리·환경변수·프레임워크 프리셋).
- 클라우드/API의 **비용 구조(지출 상한·예산·킬스위치)를 설정하지 않아** 예상치 못한 요금 폭탄을 맞는다. 이 비용은 API 키(OpenAI 등)만이 아니라 **플랫폼 인프라 과금**(Vercel 대역폭, Supabase egress, AWS 상시가동, Firebase Blaze)까지 포함한다.

### 목적
로컬 저장소를 감시하며, ① 위험한 Git 동작을 사전 차단하고 ② 배포 전 환경별 체크리스트를 자동 점검·실시간 추적하고 ③ 비용 안전장치 설정 여부를 점검·안내해서, **비개발자가 사고 없이 안전하게 저장·배포**하도록 돕는다.

### 성공 기준 (요약; 검증 가능한 형태는 §8)
- 초보자가 도구를 켜두면 위험한 Git 동작과 과금 사고를 **사전에** 막고, 배포 전 체크리스트를 **순서대로** 안내받는다.
- "이 도구를 안 썼으면 사고 났을" 케이스(main 직접 커밋, 키 노출, 상한 미설정 배포)를 실제로 차단·경고한다.

---

## 2. 무엇을 (산출물·In scope)

### 정체성
**바이브코더용 "Git 안전 + 배포 프리플라이트" 로컬 도구.**
코드 에디터(IDE)가 아니다. 사용자는 코드를 여전히 Cursor/Claude Code/VS Code에서 짜고, SafeShip은 **같은 폴더를 바라보며 옆에 뜬 "안전 조종석"**이다. 비유: GitHub Desktop + 배포 프리플라이트 대시보드.

### 형태 (배포 방식)
**B안 — npx 로컬 웹앱.** GitHub 레포로 공개 → 사용자는 자기 프로젝트 폴더에서 `npx safeship`(가칭) 한 줄 실행 → 브라우저에 로컬 GUI가 뜨고, 뒤에서 로컬 Node 서버가 git·스캔·안내를 수행. (`npm run dev`/`npx prisma studio`와 동일 패턴.)
- 코어 로직과 UI를 분리 설계해 **나중에 C안(Electron 데스크톱)으로 감싸 완전 생초보까지 포괄** 가능하게 한다.

### 아키텍처 (3계층)
```
[ 코어 엔진 (라이브러리) ]  ← 로직의 심장. B/C/MCP 어디서든 재사용
   ├─ 레포 스캐너 (파일 + git 상태 읽기)
   ├─ 룰 레지스트리 (데이터) — 리서치 3편이 그대로 들어감
   │     ├─ git-safety-rules   (위험동작 8종 + 시크릿 정규식 세트)
   │     ├─ deploy-rules       (플랫폼 어댑터 11종 + 공통 16항목)
   │     └─ cost-rules         (프로바이더 매핑 A층 + 플랫폼 과금 B층 + 상한 딥링크)
   └─ 리포트 생성기 (findings: 심각도 / 안내문구 / 클리어여부 / 딥링크)

[ npx 로컬 웹앱 (B) ]  ← v1 껍데기
   ├─ 브랜치 시각화 (dot 타임라인 + "너 지금 main이야" 경고 리본)
   ├─ 체크리스트 UI (파일 저장 시 재스캔 → 항목 ✅ 실시간 전환)
   └─ 승인 게이트 액션 (브랜치생성 / .gitignore추가 / 커밋 / 푸시 / 배포준비)
```

### 3대 기둥 (v1에 모두 포함)

**기둥 1 — Git 안전 + 시크릿·개인정보 위생**
- 위험 동작 8종 감지·차단/경고/넛지 (심각도 🔴차단/🟠경고/🟡넛지):
  main 직접 커밋 · force push · 시크릿 커밋 · 대용량 파일(node_modules/50MB+/100MB+) · `reset --hard` · 무의미 커밋 메시지 · pull 안 하고 push(diverged) · 비공개→공개 전환.
- 시크릿 스캔: 키 프리픽스 정규식(OpenAI/Anthropic/AWS/GitHub/Stripe/Google/Slack/Twilio/SendGrid/PEM/DB URL) + 엔트로피 + 파일 denylist(`.env`,`*.pem`,`serviceAccount.json`…).
- **개인정보·자격증명 위생(신규):** 사용자 계정(아이디/비밀번호)·개인정보를 **평문/약한 인코딩(base64 등)으로 저장·배포하는 것**을 감지·경고. 구체:
  - 비밀번호 해싱 부재 감지 — 인증 코드가 있는데 `bcrypt`/`argon2`/`scrypt` 등 해싱 라이브러리 미사용, 또는 사용자 파일에 평문/base64 자격증명(실제 사례: G1 auto `G1_USERS_B64`).
  - 커밋된 사용자/자격증명 파일(users.json, dump.sql에 평문 비번), PII(주민번호·전화·이메일 목록) 하드코딩·클라 노출.
  - 배포 게이트에서 "인증 사용 여부 감지 → 해싱·HTTPS·환경변수 처리 확인" 체크 항목 강제.
- 초보용 브랜치 시각화 + 무전문용어 메타포("main=출판된 책, 브랜치=초고 노트", "커밋=사진", "푸시=클라우드 업로드").
- 안전한 브랜치 모델 유도: 새 작업 = 자동 브랜치 → 작업·커밋 → 머지 → main에서 배포.

**기둥 2 — 배포 프리플라이트 (멀티 생태계)**
- **언어/프레임워크 자동 탐지 — JS에 한정하지 않음.** 스택이 정해지면(선택은 AI/사용자가) 그 스택에 맞는 체크리스트만 조립한다:
  - JS/TS: Next / Vite-SPA / CRA / Astro / SvelteKit / Nuxt / Remix / Node·Express / static
  - Python: Django / Flask / FastAPI — ⚠️ `DEBUG=True` 프로덕션 배포(시크릿·스택트레이스 공개 노출), `SECRET_KEY` 하드코딩, `ALLOWED_HOSTS='*'`, 개발서버(runserver) 배포
  - Ruby: Rails · PHP: Laravel(`APP_DEBUG`·`.env` 노출) · Go · Rust · Java(Spring, application.properties 평문 비번) · Elixir/Phoenix
  - static / no-build
  - **핵심 공통 함정:** 프로덕션 디버그 모드 ON(Django DEBUG / Flask debug / Laravel APP_DEBUG) = 시크릿·스택트레이스 공개 노출. JS의 클라이언트 키 노출에 대응하는 비-JS 최악 사고.
- 배포 타깃 탐지(설정파일: vercel.json/netlify.toml/render.yaml/fly.toml/wrangler/firebase.json/amplify.yml/Procfile/Dockerfile…).
- **공통 체크리스트 16항목**(어디든 적용): 로컬 빌드 통과 · 락파일 커밋 · node_modules ignore · .env 미커밋 · 하드코딩 시크릿 없음 · env 문서화(.env.example) · 클라 노출 변수에 시크릿 없음 · Node 버전 고정 · 출력 디렉터리 · 프로덕션 브랜치 · 프레임워크-타깃 일치 · 마이그레이션 · 플랫폼 상태 디렉터리 ignore · 의존성 클린 설치 · 무료티어/비용 sanity.
- **플랫폼별 어댑터 11종**(전부 데이터로 수용): 각 플랫폼의 탐지 신호 + 배포 전 체크리스트 + 무료티어 함정.
- 체크리스트 실시간 추적: 사용자가 항목을 해결(직접 or 승인 자동실행)하면 UI가 ✅로 전환. 미완료 항목은 **승인 게이트**로 "배포 준비 완료"를 막음.

**기둥 3 — 비용 가드 (2층 구조, "안전장치 걸었는지 체크")**
- **층위 A (종량 과금 서비스):** package.json/deps → 프로바이더 매핑(OpenAI·Anthropic·Gemini·이미지 API·Twilio·Maps 등). 위험 패턴 감지(rate limit 없음, 캐시 없음, 무한 retry, 비싼 모델 기본값).
- **층위 B (플랫폼 인프라 과금):** Vercel(대역폭·함수·이미지최적화) · Supabase(egress·컴퓨트) · Firebase(Blaze·열린 규칙) · AWS(상시가동·예산) · Render/Railway/Fly/Heroku(always-on) · Cloudflare(Workers).
- **핵심 교육 원칙:** "예산 알림 ≠ 지출 상한." 진짜 하드캡 있는 곳/없는 곳을 구분해 알려주고, 없는 곳은 **자동 킬스위치** 필요성을 안내.
- **키 노출 스캔(최고 우선순위):** 클라 노출 프리픽스(`NEXT_PUBLIC_`/`VITE_`/`REACT_APP_`/`EXPO_PUBLIC_`/`PUBLIC_`)에 담긴 시크릿, 커밋된 `.env`, `service_role` 키 클라 노출.
- **v1 처리:** 감지된 각 서비스마다 "[ ] OO 지출 상한 설정했나요? → (프로바이더 대시보드 딥링크)" 체크 항목 + 사용자 확인. 위험 패턴은 자동 경고.

---

## 3. 범위 밖 (Out of scope) — 가장 중요

- ❌ **코드 에디터(IDE) 기능** — 코드 작성/편집/언어서버/확장 생태계. 통째로 범위 밖. (Cursor/VS Code를 만드는 게 아니다.)
- ❌ **v1에서 실제 배포 실행 대행** — v1은 "이제 배포하세요" 안내까지. `vercel deploy`를 도구가 누르는 건 v2+.
- ❌ **v1에서 전체 git 히스토리 시크릿 스캔** — v1은 워킹트리+스테이징만. 단, "비공개→공개 전환" 시점에는 히스토리 스캔 필요성을 **경고는** 한다(실행은 v2). ※ 미해결 항목(§11)에서 재검토.
- ❌ **v1에서 프로바이더 API로 실시간 지출/캡 상태 조회** — v1은 딥링크 안내 + 사용자 확인 체크. 실제 조회는 v2+.
- ❌ **v1에서 데스크톱 앱(C) / MCP 서버** — 코어를 분리해두되, v1 산출물은 npx 웹앱(B)만.
- ❌ **위험한 git 작업 자동 되돌리기** — v1은 안전한 액션(브랜치생성/.gitignore/커밋/푸시)만 대행. 위험 작업은 안내만.
- ❌ **시작-시점 "스택 선택 advisor"** — "기획→어떤 프레임워크/언어로 시작할지" 추천은 AI/코딩툴이 이미 함(중복 회피). SafeShip 코어는 **배포-시점 사고 방지**에만 집중. 단, 같은 배포-지식을 역방향으로 쓰는 advisor 모드는 **나중 옵션 모듈**로 추가 가능(코어 룰 재사용).
- (범위 조정 v0.4) **비-JS 생태계는 이제 In scope** — Python/Ruby/PHP/Go/Rust/Java 등 탐지+배포 체크리스트를 데이터로 포함(`research/research-nonjs-environments.md`). 다만 각 생태계 어댑터의 심화도는 실제 샘플 검증(To-do 9)으로 점진 확장.

---

## 4. 누가 (타깃·이해관계자·역할)

- **1차 타깃:** 바이브코더(Claude Code/Cursor로 코딩, `npm run dev` 정도는 하는 비개발자). npx 한 줄 실행 가능.
- **2차 타깃(향후 C안):** 터미널이 벽인 완전 생초보. 데스크톱 설치형으로 포괄.
- **제작·운영:** coolk 혼자 + AI. 일정 무압박.
- **배포·공유:** GitHub 공개 레포 / npm 패키지로 "누구나 쉽게" 사용.

---

## 5. 어떻게 (방식·스택·제약·단계 분할)

### 기술 스택
- **런타임/백엔드:** Node.js (로컬 서버). git 접근 = `simple-git` 또는 child_process, 파일 스캔 = fast glob.
- **프론트:** React + TypeScript (Vite). 로컬 서버가 정적 서빙 + API.
- **배포/실행:** npx 실행형 npm 패키지. `npx safeship`으로 로컬 서버 기동 + 브라우저 자동 오픈.
- **코어 엔진:** 별도 패키지/모듈로 분리(`@safeship/core`). UI·MCP·데스크톱이 공유. **로직은 코어, 화면은 껍데기** 원칙 엄수.
- **룰 레지스트리:** 선언적 데이터(JSON/TS). 리서치 3편을 규칙 데이터로 이관. 플랫폼/프로바이더 추가 = 데이터 한 줄.

### 설계 원칙 (v0.5 명문화)
- **도구 불가지론(tool-agnostic):** SafeShip은 특정 AI 코딩툴에 붙지 않는다. **파일시스템·git을 감시**하므로 Claude Code든 Codex든 Copilot이든(사람 손이든) 디스크에 변경이 생기면 동작한다. **슬래시 명령어 불필요** — 한 번 설정(감시 실행 + git hook 설치)하면 백그라운드에서 자동 점검. 특정 툴 연동(MCP 등)은 부가 옵션일 뿐 코어 요건이 아니다.
- **로컬 우선(local-first) — GitHub 연동은 선택:** v1은 GitHub 계정 연동 없이 **로컬 git 저장소만으로 완전 동작**한다(워처+훅+스캔 전부 로컬 / 푸시 대상이 GitHub든 GitLab이든 무관). 코드가 사용자 컴퓨터를 떠나지 않는 것이 무료층의 셀링포인트. GitHub OAuth/App 연동은 v2+ 선택 업그레이드 — 원격 레포 설정 점검(공개여부·브랜치 보호), 비공개→공개 전환 감지, PR/CI 게이트, 팀 클라우드 모니터링(유료층)이 열린다. 비유: 로컬 = 집에 단 화재경보기, GitHub 연동 = 소방서 직통 회선.
- **UX 원칙:**
  - **표준 용어 유지 + 호버 풀이:** 브랜치·커밋·푸시·해싱 등 용어는 원어대로 표기(사용자가 용어에 익숙해지도록). 어려운 용어에는 마우스 호버/포커스 시 친절한 한 줄 풀이 툴팁. 유아어 번역("연습용 갈래" 류) 금지. **어포던스 필수:** 툴팁 용어는 볼드+글로우+점선 밑줄로 "만질 수 있음"을 드러내고, 첫 화면에서 예시 용어로 직접 체험시켜 학습("반짝이는 밑줄 단어에 마우스를 올려보세요").
  - **개념은 시각화:** 브랜치 분기→병합/폐기 흐름을 인터랙티브 다이어그램으로. "무슨 일이 있어도 main은 안전"을 눈으로 이해시켜 막연한 두려움 해소.
  - **원스크린 대시보드:** 세로 스크롤 나열이 아니라 100vh 한 화면 계기판(관제탑 컨셉). 판정(GO/NO-GO)·3기둥·게이트가 한눈에.
  - **용어+뜻+명령어 3단 표기:** 액션 버튼은 "커밋 — 지금까지 저장 `git commit`" 형태로 자연 학습 유도.
- **UX 정체성: "우주 발사" 메타포 (v0.7 확정):**
  - 화면 구조는 기능별 분류가 아니라 **초보의 시간 순서(여정)**를 따른다. 사이드바 = **발사 시퀀스 타워**(지상→궤도 세로 여정): 🛠 조립동(작업) → 📦 격납고(커밋·푸시 안전) → 🔍 발사 전 점검(배포 체크) → ⛽ 연료 점검(비용 방어) → 🚀 발사(게이트). 각 역에 신호등 상태, 로켓 아이콘이 현재 위치 표시.
  - **GPS 원칙:** 메인 상단에 항상 "지금 할 일 딱 하나"를 크게 — 초보는 순서를 결정할 필요가 없다. 완료되면 다음 안내로 자동 전환.
  - **은유 매핑:** 배포=발사(launch), 비용=연료(누수=연료 유출), 시크릿 노출=해치 열림, 브랜치=시험 비행(본선 main은 항상 무사), 배포 게이트=발사 관제 GO/NO-GO, 배포 성공="내 은하에 별 하나 추가" — 확정 카피: *"안전하게 세상에 또 하나의 별을 띄웠습니다."* (감정 보상 장치, 배포 이력=별자리).
  - **노출 시점 모델 (4 moments):** ① 코딩 중 = 침묵(화면에 안 나타남, 워처만 백그라운드) ② 커밋/푸시 순간 = 훅 검문 — 통과 시 아무것도 안 뜸, 차단 발견 시에만 터미널 요약 + 관제탑 자동/제안 오픈 ③ 배포 직전 = 사용자가 원해서 발사 시퀀스 실행 ④ 배포 후 = (v2 유료) 클라우드 모니터링 알림. 원칙: **방해 없는 기본값** — 필요한 순간에만 나타나는 안전망. 주의: 바이브코더 환경은 대부분 push=자동배포(Vercel 등)이므로 **pre-push 훅이 사실상의 발사대 관문**.
  - **경계:** 은유는 구조·이미지·보상에만. 항목 본문은 표준 용어(.env, service_role 등) 유지 + 호버 풀이 병행. 유치화 금지.
  - **프레젠테이션(랜딩·발사 시퀀스) = "함선 해부 스캔" 스크롤 인터랙션:** 세로로 긴 함선 이미지를 화면에 고정(pin)하고 스크롤 = 카메라 팬(기수→화물칸→엔진). 부위별 앵커 포인터 → 리더선 → 확대 렌즈 → **우주인 캐릭터 "관제사 NOVA"(임시명)가 말풍선으로 브리핑**. 부위=점검 매핑: 기수 해치=시크릿 밀폐 / 화물칸=Git 적재물 / 엔진·연료=비용 방어. 올그린 → GO 판정 → 발사 → 별 탄생. 피날레 = **떠나가는 별을 향해 손 흔드는 NOVA의 뒷모습**(떠나보냄의 정서). 함선·캐릭터 아트웍은 GPT-IMG2 생성 이미지로 교체 가능하게 설계(앵커는 % 좌표). 일상 관제 대시보드는 별도(컴팩트 모드). 내비게이션은 좌측 상시 메뉴 대신 **현재 구간 제목 칩 하나**(콜아웃의 SCAN 태그와 동일 문법)만 상단에 표시 — 가독성·화면 간섭 최소화.
  - **브리핑 게이트 (v0.9 — 경각심 장치, 클릭 해제 방식):** 문제(차단/경고)가 있는 구간은 **그냥 스크롤로 지나칠 수 없다.** 브리핑 카드의 **"✓ 확인했어요 — 스캔 계속" 버튼을 누르기 전에는** 아래 방향 스크롤이 그 구간 끝선에서 멈춘다(시간 경과로는 안 풀림 — 능동적 확인을 요구). 규칙: ① 위로 돌아가는 스크롤은 항상 자유 ② 확인한 구간은 다시 잡지 않음 ③ 빠른 플링으로 여러 구간을 건너뛰어도 첫 번째 미확인 구간에 걸림 ④ `prefers-reduced-motion`이면 게이트 없음(접근성) ⑤ 미확인 구간 안에서는 HUD에 "⏸ 브리핑을 확인해 주세요" 표기로 왜 멈추는지 알림. 피날레의 차단 목록은 각 항목 클릭 시 해당 구간 브리핑으로 되돌아가는 점프 링크.
  - **내 은하(My Galaxy) 대시보드 (v0.9 — 배포 이력=별자리의 구체화):** 발사(배포)에 성공할 때마다 사용자의 은하에 별이 하나씩 쌓인다. 별 = 배포된 프로젝트, 클릭 → 프로젝트·발사 이력 카드. **v1(로컬):** 발사 이력을 로컬에 기록 → 별 개수·이름·발사일만 정직하게 표시(레포만으로 알 수 있는 것만). **v2(클라우드 연동, 유료층):** 별의 밝기·맥동 = 실제 트래픽(방문/호출량, 플랫폼 API 연동), **붉은 깜빡임 = 비용 이상 신호**(지출 급증 감지) — "내 별이 살아 숨쉬는지"를 한 화면에서 본다. 부록 B의 "연속 watching 과금"과 직결되는 리텐션·업셀 장치. 원칙: 트래픽·비용 데이터는 연동 없이는 알 수 없으므로 v1에서 흉내(가짜 그래프) 금지.
- **AI 수정 위임 (fix-prompt) — v0.6 신규 코어 기능:**
  - 수정 경로를 둘로 나눈다. **단순·안전한 수정**(.gitignore 추가, 브랜치 생성 등) = 도구가 직접 실행. **코드 변경이 필요한 수정**(키를 서버로 옮기기, rate limit 추가 등) = 도구가 직접 하지 않고, **사용자가 쓰는 AI에 붙여넣을 완성형 프롬프트를 생성**해 "AI로 고치기 → 프롬프트 복사" 버튼 제공.
  - 프롬프트에는 문제 설명 + 해결 단계 + 주의사항(예: "키 값은 출력하지 마", "재발급 절차 안내")을 포함하되, **시크릿 실제 값은 절대 포함하지 않는다**(마스킹 원칙과 동일).
  - 근거: ① 위험한 자동 리팩터링을 도구가 떠안지 않음 ② 초보의 진짜 장벽은 "AI에게 뭐라고 물어야 할지 모름" — 이를 해소 ③ 도구 불가지론과 일치(Claude/Cursor/Copilot 어디든 붙여넣기 가능).

### 핵심 제약
- **레포 스캔의 한계:** "대시보드에서 캡을 걸었는지"는 레포에 없음 → v1은 안내+확인체크로 처리(정직하게 표기).
- **로컬 전용:** 브라우저 보안상 로컬 파일·git 접근은 로컬 서버(B) 또는 네이티브(C)로만 가능. 순수 호스팅 웹(A)은 불가 → 채택 안 함.
- **파괴적 동작 금지:** 도구가 사용자 승인 없이 커밋 삭제·force push·파일 삭제를 하지 않는다.

### 단계 분할 (마일스톤은 §6)
M0 코어 엔진 뼈대 → M1 Git 안전 → M2 배포 프리플라이트 → M3 비용 가드 → M4 UI 통합·실시간 추적 → M5 승인 게이트 액션 → M6 패키징·공개.

---

## 6. 언제 (마감·마일스톤)

**마감 없음(무압박).** 날짜 대신 완료 기준으로 끊는 작은 마일스톤:

| MS | 내용 | 완료 기준 |
|----|------|-----------|
| M0 ✅ | 코어 엔진 뼈대 + 레포 스캐너 + 리포트 스키마 | 임의 레포 폴더 → findings JSON 출력 — **완료 (2026-07-09)**: `packages/core`(타입·컨텍스트·스택탐지·룰 9종·스캔) + `packages/cli`(safeship scan/--json), `samples/demo-next` 픽스처에서 목업 시나리오 재현(NO-GO·차단 5·fixPrompt 6) 검증 |
| M1 | git-safety-rules + 브랜치 시각화 데이터 | 위험 동작 8종 감지, 시크릿 스캔 동작 |
| M1.5 ✅ | **pre-push 훅 게이트** (`safeship init`) | **완료 (2026-07-19)**: push 직전 자동 스캔 — GO면 조용히 통과, NO-GO면 터미널 리포트+브라우저 관제탑 자동+push 차단(exit 1). 우회 `git push --no-verify`. 훅은 3단계 폴백(global/npx/절대경로), 기존 pre-push 훅 보존. 로컬 원격으로 차단→해결→통과 전 사이클 검증 |
| M2 ✅ | deploy-rules(공통 + 언어/플랫폼 어댑터) + 프레임워크/타깃 탐지 | **완료 (2026-07-10)**: 비-JS 매니페스트(deps) 흡수, 디버그모드(django/flask/fastapi/laravel/rails)·프로덕션서버·포트바인딩·SECRET_KEY/master.key·CORS·마이그레이션·플랫폼설정시크릿(wrangler/fly/netlify/render)·상태폴더·언어별 락파일. 샘플 3종(next/flask/django)에서 타깃·체크리스트 정확 산출 검증. 잔여(Spring/.NET/Elixir, 출력디렉터리/SPA fallback, 빌드 실행 검사)는 §11 |
| M3 ✅ | cost-rules(A층+B층) + 키 노출 스캔 + 딥링크 | **완료 (2026-07-10)**: 프로바이더 감지 pip/gem/composer/go까지 확장, CAP_GUIDES 18종(+render/railway/fly/heroku/netlify/cloudflare/amplify/sendgrid), rate limit 룰 python/ruby 확장(flask-limiter/slowapi/django-ratelimit/rack-attack 레시피) |
| M4 | npx 웹앱 UI + 파일변경 재스캔 실시간 추적 | 브라우저에서 체크리스트 ✅ 전환 확인 |
| M5 | 승인 게이트 액션(브랜치/커밋/푸시/.gitignore) | UI 버튼으로 안전 액션 실행 |
| M6 | npm 패키징 + GitHub 공개 + README | `npx safeship` 타인 환경에서 실행 |

---

## 7. 어디서 (환경·플랫폼·자원 위치)

- **실행 환경:** 사용자 로컬 머신(Win/Mac/Linux), Node 설치 전제. 브라우저에서 GUI.
- **배포/유통:** GitHub 공개 레포 + npm.
- **지원 배포 타깃(도구가 점검하는 대상):** Vercel, Netlify, Cloudflare Pages/Workers, Render, Railway, Fly.io, Heroku, GitHub Pages, Firebase Hosting, Supabase, AWS Amplify, 범용 static. (전부 데이터로 수용, 신규 추가 용이.)
- **자원:** 외부 서비스 호출 없음(v1). 모든 스캔은 로컬. 리서치 지식 베이스는 `research/`에 동봉.

---

## 8. 성공 기준 (검증 가능하게)

- [ ] 임의 프로젝트 폴더에서 `npx safeship` 실행 시 3초 내 브라우저에 현재 브랜치·체크리스트가 렌더링된다.
- [ ] 사용자가 `main`에 있을 때 커밋 시도하면 경고 리본 + "안전 브랜치 만들기" 제안이 뜬다.
- [ ] 스테이징에 `.env` 또는 키 프리픽스 문자열이 있으면 🔴차단 수준으로 감지하고, .gitignore 추가를 제안한다.
- [ ] Next.js+Vercel 레포에서 출력 디렉터리·환경변수·`NEXT_PUBLIC_` 시크릿 노출·프로덕션 브랜치 항목이 정확히 체크리스트로 나온다.
- [ ] `openai`/`@supabase/supabase-js`/Firebase 등이 감지되면 각각의 "지출 상한 설정했나요? + 딥링크" 항목이 생성된다.
- [ ] 파일을 수정·저장하면 관련 체크 항목이 자동 재평가되어 ✅/❌가 갱신된다.
- [ ] 미완료(🔴/🟠) 항목이 있으면 "배포 준비 완료" 게이트가 열리지 않고, 사용자 승인 시에만 진행된다.

---

## 9. 가드레일 (작업 에이전트가 하지 말아야 할 것)

- 사용자 승인 없이 **파괴적 git 동작**(force push, 커밋 삭제, reset --hard, 파일 삭제) 실행 금지.
- **로직을 UI에 하드코딩 금지** — 모든 점검 로직은 코어 엔진/룰 레지스트리에. UI는 표시·입력만.
- **레포에 없는 정보를 "확인했다"고 단정 금지** — 캡 설정 여부 등은 사용자 확인 체크로만 처리, 자동 통과시키지 않는다.
- **외부 API 키를 도구가 저장·전송 금지**(v1). 실시간 지출 조회는 v2에서 별도 보안 설계 후.
- 시크릿을 로그·리포트에 **평문 출력 금지**(마스킹).
- 룰 데이터는 `research/` 3편을 근거로 하되, **수치·한도는 volatile** — 코드에 하드코딩된 가격/한도는 출처 주석 필수, 신뢰성 검증 후 반영.
- 스캔 대상 폴더 밖 파일 읽기/쓰기 금지.

---

## 10. 다음 단계 작업 분할 (To-do)

1. `@safeship/core` 패키지 스캐폴딩 — 레포 스캐너 인터페이스 + findings 스키마(심각도/카테고리/안내/딥링크/clearable) 정의.
2. 룰 레지스트리 포맷 설계 후 리서치 3편 → 규칙 데이터 이관 (git-safety / deploy / cost).
3. 프레임워크·배포타깃 탐지기 구현 (research-deploy-platforms.md §0 우선순위 로직).
4. 시크릿 스캐너 구현 (정규식 세트 + 엔트로피 + denylist + 마스킹).
5. Git 상태 분석기 (현재 브랜치, staged 파일, diverged, diff 크기).
6. npx 실행 엔트리 + 로컬 서버 + 브라우저 자동 오픈.
7. React UI: 브랜치 리본/타임라인, 체크리스트 뷰, 승인 게이트, 파일변경 watch→재스캔.
8. 안전 액션 실행기(브랜치생성/.gitignore/커밋/푸시) — 승인 게이트 경유.
9. 샘플 테스트 레포 세트(각 프레임워크×타깃) + 골든 스냅샷 검증.
10. 패키징·README·GitHub 공개.

---

## 11. 미해결·가정 (남은 리스크)

- **가정:** 1차 사용자는 Node가 설치돼 있고 npx를 실행할 수 있다. (아니면 C안 우선순위 상승.)
- **미해결(재검토 대상):** 전체 히스토리 시크릿 스캔을 v1에 넣을지. "비공개→공개 전환" 사고는 가장 위험한 케이스이며 사용자가 이 부분을 특히 우려 → M1에서 "경고만 v1 / 실행 v2" 경계를 실제 성능 측정 후 재결정.
- **미해결:** 실시간 지출 조회(v2)의 키 보관·인증 보안 설계.
- **미해결:** 제품명(SafeShip) 확정 및 npm 패키지명 확보.
- **volatile:** 무료티어 한도·가격은 수시 변동 → 룰 데이터에 출처·검증일 병기, 주기적 갱신 필요.
- **범위 감시:** "전체 배포환경 수용"은 데이터로 달성하되, 어댑터 정확도는 플랫폼별 실 테스트 없이는 보장 못 함 → 샘플 레포 검증(To-do 9) 필수.
- **전수검사 백로그 (2026-07-10 리뷰 3종 — 정확성·시크릿 품질·커버리지 감사에서 나온 잔여):**
  ① Spring Boot/.NET/Phoenix 어댑터(언어 타입 확장 필요) ② 출력/퍼블리시 디렉터리 ↔ 프레임워크 기본값 대조표 ③ git 히스토리 시크릿 스캔(M1 훅/명령) ④ 프리픽스 없는 generic 시크릿(엔트로피 기반 — 오탐 설계 선행) ⑤ next/image·ISR/cron·Firestore 무제한 쿼리 휴리스틱 ⑥ PII(SQL/CSV 덤프) 스캔 ⑦ isIgnored 배치화(check-ignore --stdin) ⑧ 300KB 초과 파일 스킵 고지 ⑨ rate limit 라우트 수준 검사 + 캐시/재시도 백오프 검사 ⑩ Cloudflare compatibility_date 등 플랫폼 세부 체크 잔여.

---

## 부록 A. 실제 검증 케이스 (골든 테스트) — 작성자 본인 프로젝트

과거 세션 기록에서 추출한 실제 사고/수정 사례. SafeShip이 배포 전에 잡았어야 할 케이스로, To-do #9 검증의 근거 데이터로 사용한다. (출처: 로컬 세션 전문 검색, 2026-07-08 추출)

| # | 실제로 있었던 일 | 프로젝트 | SafeShip 판정 | 대응 룰 |
|---|---|---|---|---|
| 1 | `g1-web/.env`에 `SESSION_SECRET`·`G1_USERS_B64`·네이버 API key가 평문 + git 추적("M-5 위험") | G1 auto | 🔴 차단 | 기둥1 시크릿스캔(.env denylist + git 추적) |
| 2 | 사용자 계정을 base64(`G1_USERS_B64`)로만 저장 = 사실상 평문(해싱 없음) | G1 auto | 🔴 차단 | 기둥1 개인정보·자격증명 위생(해싱 부재) |
| 3 | "기능 외로 API가 누수되는 부분… 금액 누수" — fable로 사후 리뷰 | gym_manager | 🟠 경고 | 기둥3-A 프로바이더 감지 + rate limit/캐시 없음 |
| 4 | GitHub 푸시 전 "API key/token/password 미검출" 수동 시크릿 스캔 | AIB 개발용어사전 | ✅ 자동화 대상 | 기둥1 = 이 수동 리뷰의 자동·강제화 |
| 5 | Supabase `anon`(공개 OK) vs `service_role`(노출 금지) 키 구분 | 네이버 검색순위 웹앱 | 🔴 차단 | 기둥3-B service_role 클라 노출 감지 |
| 6 | 배포 인프라 선택: Cloudflare(한국 우회) vs AWS 서울리전 | AIB 랜딩페이지 | ⚪ 안내 | 기둥3-B 플랫폼 과금·성능 가이드 |

**시사점:** 작성자 본인이 최소 5개 프로젝트에서 SafeShip이 자동화할 점검을 **수동/사후 리뷰로 반복**해왔다. 전문가도 매번 리뷰를 돌려야 발견하는 것을, 초보는 리뷰를 돌릴 생각조차 못 한다 — 이 도구의 존재 이유.

---

## 부록 B. 수익화 모델 (출처: research/research-monetization.md)

### 핵심 통찰
**창작 도구(Cursor·Lovable·v0·Bolt)는 AI 추론/크레딧을 판다. 안전 도구(GitGuardian·Snyk·Socket)는 "연속 보호 + 팀 + 컴플라이언스"를 판다.** SafeShip은 아무것도 생성·호스팅하지 않는 **가드레일**이므로 **안전 도구 플레이북**을 따른다. 표어: **"스캔은 공짜로 주고, '지켜봐 주는 것(watching)'에 과금한다."**

### 채택 모델: 오픈코어 + 프리미엄 SaaS (per-repo 중심)

**무료 (채택 엔진 — 반드시 훌륭해야):**
- `npx safeship` 로컬 실행 전체: 배포 전 체크리스트, Git 안전, 시크릿·개인정보 스캔, 1회성 비용/설정 sanity, 로컬 pre-commit/pre-deploy 훅.
- OSS(MIT) 공개 — 비개발자·바이브코더 커뮤니티의 신뢰와 입소문 확보.

**유료 "SafeShip Cloud" (반복 수익):**
1. **연속 클라우드 모니터링** — 1회 스캔이 아니라 매 푸시/배포마다 재스캔. (구독의 핵심 = "one-off CLI"와 "구독"의 결정적 차이)
2. **실시간 지출/비용 가드 대시보드** — Vercel/AWS/Supabase 결제 연동, 비용 급증·폭주 함수 알림. ⭐ **경쟁사(GitGuardian/Snyk)가 안 하는 SafeShip만의 차별점** = 바이브코더의 공포를 직접 겨냥.
3. **팀/멀티레포** — 한 대시보드에서 여러 레포. ⭐ **에이전시 킬러 기능**(고객사 30개 레포 관제, 고객별 리포트).
4. **CI/CD 배포 게이트** — 미통과 시 배포 차단, 정책 규칙, GitHub Actions 연동.
5. **데스크톱 앱 / 원클릭 "배포 OK" 버튼** — 터미널 거부하는 비개발자용.

**가격 (경쟁사 앵커링):**
- **Free** — 로컬 CLI + 레포 1개.
- **Pro(솔로) ~$12–20/mo** — 연속 모니터링 + 지출 대시보드 + 데스크톱, 레포 3~5개. (Copilot/Cursor Pro와 같은 체감가)
- **Team/Agency ~$25–45/seat 또는 ~$8–15/모니터링 레포/mo** — 멀티레포·CI게이트·고객리포트·SSO. **에이전시는 per-seat보다 per-repo가 전환 잘 됨**(개발자 수가 아니라 고객 프로젝트 수로 사고하므로).
- **Business/Enterprise — 협의** — SSO, 감사로그, 온프렘, 컴플라이언스.

### 포지셔닝 원칙 (v0.8 확정): "IDE가 아니라, 모든 IDE 옆의 안전층"
- SafeShip은 **IDE가 되지 않는다.** 코드 작성 시장(Cursor/VS Code)은 거대 자본의 레드오션이고, 우리의 개입 지점(git hook·파일 워처·MCP)은 에디터를 소유하지 않아도 전부 작동한다.
- 비유: **Grammarly는 워드프로세서를 만들지 않았다** — 모든 편집기에 붙었다. SafeShip은 "배포의 Grammarly".
- 소유할 순간: 코드가 써진 **직후**, 세상에 나가기 **직전** — "이거 올려도 돼?"의 순간. 이 순간은 Cursor도, Vercel도, GitGuardian도 소유하지 않는다.
- 유통은 그들 위에 올라탄다: npx(코어) + VS Code/Cursor **확장** + **MCP 서버** + GitHub App — 전부 같은 코어 엔진의 껍데기(설계 원칙 §5와 일치).

### 차별화 쐐기(wedge)
(a) **비개발자용**(GUI·평이한 설명·원클릭 안전배포) + (b) **비용/지출 가드**(보안 경쟁사 미제공). **에이전시 멀티레포 모션에 반복 수익이 집중** → 유료 티어를 처음부터 per-repo/per-client 관리 중심으로 설계. **작성자의 AIB 사업이 첫 유료 고객 후보**(도그푸딩 + 세일즈 동시).
```
